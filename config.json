{
  "name": "prometheus-connector-2",
  "config": {
    "topics": "metrics-2",
    "confluent.topic.bootstrap.servers": "localhost:9092",
    "prometheus.listener.url": "http://localhost:8080/metrics",
    "reporter.result.topic.replication.factor": "1"
  }
}
